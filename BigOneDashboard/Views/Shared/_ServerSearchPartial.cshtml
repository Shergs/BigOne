@using Microsoft.AspNetCore.Identity
@using System.Security.Claims;
@using Microsoft.AspNetCore.Authentication;

@* @if (User.Identity.IsAuthenticated)
{ *@
@*     <li>
 *@     <div class="flex items-center justify-center">
            <div class="relative group">

                <button id="dropdown-abutton" type="button" class="text-white bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 font-medium rounded-lg text-sm px-4 py-2 text-center dark:bg-blue-600 dark:hover:bg-blue-700 dark:focus:ring-blue-800">Select Server</button>
                    @* <span class="mr-2">Select Server</span>
                    <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5 ml-2 -mr-1" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
                        <path fill-rule="evenodd" d="M6.293 9.293a1 1 0 011.414 0L10 11.586l2.293-2.293a1 1 0 111.414 1.414l-3 3a1 1 0 01-1.414 0l-3-3a1 1 0 010-1.414z" clip-rule="evenodd" />
                    </svg> *@
                </button>
                <div id="dropdown-menu" class="hidden absolute right-0 mt-2 rounded-md shadow-lg bg-white ring-1 ring-black ring-opacity-5 p-1 space-y-1 dark:!bg-gray-900 dark:!text-white">
                    <!-- Search input -->
                    <input id="search-input" class="block w-full px-4 py-2 text-gray-800 border rounded-md  border-gray-300 focus:outline-none dark:!bg-gray-900 dark:!text-white" type="text" placeholder="Search items" autocomplete="off">
                    <!-- Dropdown content goes here -->
                    @foreach (Guild guild in Model.AvailableGuilds)
                    {
                        <a class="block px-4 py-2 text-gray-700 hover:bg-gray-100 active:bg-blue-100 cursor-pointer rounded-md"a data-dropdown-element="server" href="@Url.Action("Index","Home", new {Area = "", serverId = guild.Id})">@guild.Name</a>
                    }
                </div>
            </div>
        </div>
    @* </li> *@
@* } *@
